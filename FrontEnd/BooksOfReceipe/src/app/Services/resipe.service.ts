import { Injectable } from '@angular/core';
import { User } from '../Entityes/user';

@Injectable({
  providedIn: 'root'
})
export class ResipeService {

  constructor() { }

  getRecypeById(id: number) {

  }

  getUserRecypes(user: User) {
    
  }
}
